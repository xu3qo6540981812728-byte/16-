{
  "name": "real-estate-mbti-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "房仲16型人格測驗（Netlify 靜態站 + Functions）",
  "scripts": {
    "dev": "netlify dev",
    "build": "node scripts/write-firebase-web-config.js"
  },
  "devDependencies": {
    "netlify-cli": "^19.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
